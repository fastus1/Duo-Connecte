---
phase: 03-documentation-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true

must_haves:
  truths:
    - "Developer can find installation instructions in README"
    - "Developer can find all environment variables documented"
    - "Developer can find deployment instructions"
  artifacts:
    - path: "README.md"
      provides: "Complete developer documentation"
      min_lines: 150
      contains: ["## Installation", "## Configuration", "## Deployment"]
  key_links:
    - from: "README.md"
      to: ".env.example"
      via: "Reference to environment file"
      pattern: "\\.env\\.example"
    - from: "README.md"
      to: "ARCHITECTURE.md"
      via: "Reference to architecture docs"
      pattern: "ARCHITECTURE\\.md"
---

<objective>
Create comprehensive README.md with installation, configuration, and deployment instructions.

Purpose: Developers cloning this template need clear guidance to get started quickly.
Output: README.md at project root covering all essential documentation.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-documentation-data/03-RESEARCH.md
@.env.example
@ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create README.md with complete documentation</name>
  <files>README.md</files>
  <action>
Create README.md at project root with the following sections:

1. **Header**: Circle App Template title, brief tagline

2. **Overview** (2-3 sentences): Ready-to-deploy Circle.so app infrastructure with SSO, PIN auth, paywall, admin dashboard

3. **Features** (bullet list):
   - Circle.so SSO integration via postMessage
   - Optional PIN authentication
   - Paywall support for paid content
   - Admin dashboard for configuration
   - Support ticket system
   - User management

4. **Prerequisites**:
   - Node.js 18+
   - PostgreSQL database (Neon recommended)
   - Circle.so community with Custom Apps enabled

5. **Installation** (numbered steps):
   - Clone repository
   - Install dependencies: `npm install`
   - Copy .env.example to .env
   - Configure environment variables (reference Configuration section)
   - Push database schema: `npm run db:push`
   - Start development: `npm run dev`

6. **Configuration** (environment variables table):
   Document ALL variables from .env.example with columns:
   | Variable | Required | Description | Example |
   Include: DATABASE_URL, JWT_SECRET, VITE_CIRCLE_ORIGIN, VITE_DEV_MODE, SESSION_TIMEOUT, PIN_ATTEMPTS_LIMIT, PIN_ATTEMPTS_WINDOW, PORT, NODE_ENV

7. **Usage**:
   - Development: `npm run dev` (starts on PORT, default 5000)
   - Production build: `npm run build && npm start`
   - Database migrations: `npm run db:push`

8. **Project Structure** (brief tree):
   ```
   /
   ├── client/          # React frontend
   ├── server/          # Express backend
   ├── shared/          # Shared types/schema
   └── ARCHITECTURE.md  # Detailed architecture docs (French)
   ```

9. **Extension**: Reference to docs/extension-guide.md (created in Plan 02)

10. **Deployment**:
    - Railway: Set DATABASE_URL and env vars, deploy
    - Replit: Import from GitHub, add secrets, run
    - Generic: Build, set NODE_ENV=production, provide DATABASE_URL

11. **Circle.so Setup**:
    - Brief explanation: This app runs as iframe inside Circle
    - Steps: Custom Apps > Create App > Set URL > Enable in space
    - Note: VITE_CIRCLE_ORIGIN must match community URL

12. **License**: MIT

Language: English, professional but accessible tone.
Length target: 150-200 lines.
Do NOT include badges, contributors section, or changelog (keep it focused).
  </action>
  <verify>
    - File exists: `ls README.md`
    - Contains required sections: `grep -c "## Installation\|## Configuration\|## Deployment" README.md` returns 3
    - References .env.example: `grep ".env.example" README.md`
    - References ARCHITECTURE.md: `grep "ARCHITECTURE" README.md`
    - Line count: `wc -l README.md` shows 150+
  </verify>
  <done>README.md exists with installation, configuration (all env vars), deployment, and Circle.so setup instructions</done>
</task>

</tasks>

<verification>
1. README.md exists at project root
2. All sections present (Overview, Features, Prerequisites, Installation, Configuration, Usage, Structure, Deployment, Circle.so Setup, License)
3. All environment variables from .env.example are documented
4. Cross-references to ARCHITECTURE.md and extension guide included
5. No broken links or placeholder text
</verification>

<success_criteria>
- README.md is 150+ lines
- Contains complete installation workflow
- All 9 environment variables documented with descriptions
- Deployment instructions for Railway, Replit, and generic
- Circle.so integration setup explained
</success_criteria>

<output>
After completion, create `.planning/phases/03-documentation-data/03-01-SUMMARY.md`
</output>
